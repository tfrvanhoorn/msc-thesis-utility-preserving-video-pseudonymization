# --- Core Base Requirements ---
pyyaml>=6.0
pillow>=9.5.0
stringzilla==3.11.3
onnxruntime-gpu==1.14.1
pandas<2.1.0          # Pinned: pandas>=2.1.0 requires Python 3.9+
typing_extensions>=4.7.1
facenet-pytorch==2.4.1
ninja==1.11.1.4
psutil
imageio>=2.31
av>=12.0
diffusers==0.27.2
transformers==4.38.2
accelerate==0.28.0
huggingface-hub==0.22.2

# --- Modified Requirements (Version Downgrades) ---
numpy==1.23.5         # Downgraded for SimSwap/ONNX object array compatibility
insightface==0.7.3
opencv-python-headless==4.10.0.84
mediapipe==0.10.8

# --- Added Requirements (Specific to SimSwap) ---
moviepy==1.0.3        # Required for video frame management
imageio-ffmpeg        # Backend for moviepy
onnx==1.14.1          # Pinned to match onnxruntime-gpu version
timm==0.5.4           # Dependency for the SimSwap model architecture
tqdm                  # Progress bar for long swap operations
lpips==0.1.4          # Learned Perceptual Image Patch Similarity metric
scikit-image          # Provides skimage.metrics.structural_similarity (SSIM)

# --- fastai & Face-Adapter Requirements ---
fastai                # Required by Face-Adapter pipeline module import
spacy<3.8.0           # Pinned: prevents NumPy 2.0 build crash in blis dependency
scipy<1.11.0          # Pinned: scipy>=1.11.0 requires Python 3.9+
matplotlib<3.8.0      # Pinned: matplotlib>=3.8.0 requires Python 3.9+
einops==0.7.0         # Pinned: Stable version for Spring 2024 PyTorch ecosystems
safetensors==0.4.2    # Pinned: Compatible with huggingface-hub==0.22.2 and transformers==4.38.2
omegaconf==2.3.0      # Pinned: Standard stable version for Stable Diffusion pipelines
peft==0.10.0          # Pinned: Compatible with huggingface-hub==0.22.2

# --- Core PyTorch (Uncomment if needed, or keep your local install) ---
# torch==1.8.1
# torchvision==0.9.1
# torchaudio==0.8.1